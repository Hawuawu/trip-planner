// Must match the client's FUNCTIONS_REGION in src/data/firebaseTripRepository.ts
// and firebase.json's Firestore `location` (chosen for latency, not arbitrary).
export const REGION = 'europe-west1';
